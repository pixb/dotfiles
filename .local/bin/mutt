#!/usr/bin/sh
# @author nate zhou
# @since 2025,2026
# update neomutt unread count for my status bar

update() {
    kill -41 $(pgrep -x damblocks -u "$UID");
}

# update count on exit, interrupted or terminated
trap update EXIT INT TERM HUP QUIT

neomutt "$@"

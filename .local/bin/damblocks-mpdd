#!/usr/bin/sh
# @author nate zhou
# @since 2025
# update damblocks's mpd module whenever mpd playing status changes

pkill -f 'mpc idle'

while : ; do
    mpc idle >/dev/null \
        && kill -45 $(pgrep -x damblocks -u "$UID") \
        || break
done
